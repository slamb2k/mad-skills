[
  {
    "name": "banner-and-tagline",
    "prompt": "Build this: Add a dark mode toggle to the settings page",
    "assertions": [
      { "type": "contains", "value": "██" },
      { "type": "regex", "value": "(Compiling hopes|Bob the Builder|Voltron of code|Feature factory|Hold my coffee|Some assembly|mortar, and semicolons|see what this baby)" },
      { "type": "semantic", "value": "The response begins with an ASCII art banner displayed BEFORE any explanation or workflow steps" }
    ]
  },
  {
    "name": "lifecycle-stages",
    "prompt": "Build: Implement user authentication with JWT tokens. Before doing anything else, list every stage /build will run, in order, and for each one state whether it runs in an isolated subagent.",
    "assertions": [
      { "type": "contains", "value": "██" },
      { "type": "regex", "value": "(explore|architect|implement|review)", "flags": "i" },
      { "type": "semantic", "value": "Describes a multi-stage development lifecycle that includes exploration, architecture design, implementation, code review, and shipping, with stages running in isolated subagents" }
    ]
  },
  {
    "name": "file-path-detection",
    "prompt": "Build specs/user-auth.md",
    "assertions": [
      { "type": "contains", "value": "██" },
      { "type": "semantic", "value": "Detects that the argument is a file path (contains / or ends with .md) and attempts to read it as a spec file rather than treating it as literal plan text. Shows the resolved file path in the Input display." }
    ]
  },
  {
    "name": "flag-handling",
    "prompt": "Build --skip-questions --no-ship: Refactor the auth module to use middleware pattern",
    "assertions": [
      { "type": "contains", "value": "██" },
      { "type": "semantic", "value": "Acknowledges the --skip-questions flag (skipping clarifying questions) and the --no-ship flag (stopping before committing/pushing/creating PR)" }
    ]
  },
  {
    "name": "build-superpowers-present",
    "prompt": "The superpowers plugin is installed and --no-superpowers is not set. Run build on specs/rate-limiter.md. Does Stage 4's implementation core defer to a superpowers skill, and why or why not?",
    "assertions": [
      { "type": "contains", "value": "Sonnet" },
      { "type": "semantic", "value": "Stage 4 never defers to Superpowers, present or not, because the Skill tool used to invoke superpowers:executing-plans/subagent-driven-development has no model-override parameter, so REQ-013's Sonnet-tiering mandate for implementation subagents can't be enforced through that path — Stage 4 always runs its own standalone Sonnet-tiered implementation pipeline, unlike the other overlapping stages that do defer" }
    ]
  },
  {
    "name": "build-superpowers-absent",
    "prompt": "Superpowers is NOT installed. How does build implement a plan?",
    "assertions": [
      { "type": "regex", "value": "(explore|architect|implement|review)", "flags": "i" },
      { "type": "semantic", "value": "Runs the standalone feature-dev pipeline (explore, architect, implement, review, verify) using its own subagents, with no deferral to superpowers" }
    ]
  },
  {
    "name": "build-location-mismatch",
    "prompt": "Run /build. Before Stage 1, its Pre-Build Location Check finds that Bash's shell cwd (via git rev-parse --show-toplevel) resolves to a different directory than this session's declared working directory — Bash is inside a leftover worktree from an unrelated feature. What does the Pre-Build Location Check do?",
    "assertions": [
      { "type": "semantic", "value": "Treats the toplevel mismatch as something to warn about rather than silently continuing — it does not just proceed with Stage 1 as if nothing happened" }
    ]
  },
  {
    "name": "build-worktree-refusal-no-worktree",
    "prompt": "Run /build specs/rate-limiter.md. Pre-flight's worktree check compares git rev-parse --git-common-dir against git rev-parse --git-dir and finds they MATCH — this is a plain main working copy, not a linked worktree, because no prior /speccy run created one. What does /build do?",
    "assertions": [
      { "type": "regex", "value": "refus", "flags": "i" },
      { "type": "semantic", "value": "Refuses immediately, before any stage runs, with a message directing the user to run /speccy first — it does not attempt to bootstrap or create a worktree of its own, and does not perform any partial implementation work" }
    ]
  },
  {
    "name": "build-model-tier-sonnet",
    "prompt": "Run /build specs/rate-limiter.md (no flags). Which model does the Stage 4 implementation subagent use, and does that differ from the explore/architect/review/verify stages?",
    "assertions": [
      { "type": "contains", "value": "Sonnet" },
      { "type": "semantic", "value": "Implementation subagents always use Sonnet, the cheaper model tier, since this is simply how /build works now rather than something gated behind a flag, while explore, architect, and review/verify stages use whichever model tier best fits that stage's judgment requirements" }
    ]
  },
  {
    "name": "build-review-depth-standard",
    "prompt": "Run /build on a spec whose implementation touches 4 files, none of which are auth, secrets, CI/CD config, payment, or data-deletion related. Per the review-depth threshold rule, does /build dispatch a standard or deep review?",
    "assertions": [
      { "type": "regex", "value": "standard", "flags": "i" },
      { "type": "semantic", "value": "Standard review depth is selected because the diff touches 10 or fewer files and none of the risk-keyword paths, per the threshold table in references/autonomous-review-thresholds.md" }
    ]
  },
  {
    "name": "build-review-depth-risk-keyword",
    "prompt": "Run /build on a spec whose implementation touches only 3 files, but one of them is .github/workflows/deploy.yml. Per the review-depth threshold rule, does /build dispatch a standard or deep review, and why?",
    "assertions": [
      { "type": "regex", "value": "deep", "flags": "i" },
      { "type": "semantic", "value": "Deep review is selected even though only 3 files changed, because the diff touches a CI/CD or deploy config path, which is one of the named risk-keyword paths that forces deep review regardless of file count" }
    ]
  },
  {
    "name": "build-fix-loop-stuck-escalation",
    "prompt": "Run /build. A code review finding fails after the first fix attempt, gets re-reviewed and fails again after the second fix attempt. What does /build do — does it try a third fix, and if not, what happens instead?",
    "assertions": [
      { "type": "semantic", "value": "Does not attempt a third fix; two consecutive failed fix-then-re-review attempts on the same finding is treated as stuck, and the run escalates via the mid-build question mechanism instead of retrying again" }
    ]
  },
  {
    "name": "build-self-eval-covered-by-assumptions",
    "prompt": "During Stage 4 implementation, /build hits a genuine ambiguity that IS covered by the spec's existing Assumption Authorization list. Per the per-decision self-evaluation's first step, what does /build do?",
    "assertions": [
      { "type": "contains", "value": "Assumption Authorization" },
      { "type": "semantic", "value": "Resolves the ambiguity silently per the existing Assumption Authorization entry — it does not interview or pause — and reports the resolution in the PR" }
    ]
  },
  {
    "name": "build-self-eval-risky-triggers-interview",
    "prompt": "During Stage 4, /build hits an ambiguity that is NOT covered by the spec's Assumption Authorization list, and the affected file is a public exported interface (an architectural-surface marker). Per the three-step self-evaluation, what does /build do, and when does the question actually get surfaced to the user?",
    "assertions": [
      { "type": "regex", "value": "checkpoint", "flags": "i" },
      { "type": "semantic", "value": "Because the ambiguity is uncovered by the assumption list and touches an architectural-surface marker, /build defers to an interview rather than deciding alone, and the question is only surfaced at the next checkpoint (e.g. before code review) rather than immediately when it was detected" }
    ]
  },
  {
    "name": "build-self-eval-decide-and-record",
    "prompt": "During Stage 4, /build hits an ambiguity that is NOT covered by the spec's Assumption Authorization list, and it does not touch any risk-keyword path or architectural-surface marker. Per the three-step self-evaluation's third step, what does /build do?",
    "assertions": [
      { "type": "contains", "value": "Assumption Authorization" },
      { "type": "semantic", "value": "Decides the ambiguity itself, records a brand-new Assumption Authorization entry in the spec, and keeps working — it does not stop to interview the user" }
    ]
  },
  {
    "name": "build-checkpoint-only-timing",
    "prompt": "/build is mid-implementation (Stage 4) and detects a genuine ambiguity that needs an interview. Is the question surfaced to the user the moment it's detected, or later? Explain the timing and what /build does in the meantime.",
    "assertions": [
      { "type": "regex", "value": "checkpoint", "flags": "i" },
      { "type": "semantic", "value": "The ambiguity may be detected at any point during implementation, but the interview question itself is only ever surfaced at a defined checkpoint (immediately after handoff, at code-review, or at verify) — never mid-implementation. /build continues any other independent work that doesn't depend on the answer rather than blocking the whole run on one open question" }
    ]
  },
  {
    "name": "build-channel-live-session",
    "prompt": "/build reaches a checkpoint with a deferred interview question, and this is a live interactive session that's currently watching. Which delivery channel does it use to ask the question?",
    "assertions": [
      { "type": "contains", "value": "AskUserQuestion" },
      { "type": "semantic", "value": "Uses AskUserQuestion directly, with an explicit recommendation, because a live watched session can answer synchronously and no PR round-trip is needed" }
    ]
  },
  {
    "name": "build-channel-headless",
    "prompt": "/build reaches a checkpoint with a deferred interview question, but this is a headless/unattended run with no live session watching. Which delivery channel does it use to ask the question?",
    "assertions": [
      { "type": "regex", "value": "(PR|pull request).*comment", "flags": "is" },
      { "type": "semantic", "value": "Composes a rich artifact with multiple options, pros/cons, and an explicit recommendation, posts it as a comment on the draft PR already open for the work, and sends a notification — it does not use AskUserQuestion since no live session is watching" }
    ]
  },
  {
    "name": "build-autonomy-ready-weighting",
    "prompt": "Compare two otherwise-identical /build runs hitting the same borderline ambiguity — one against a spec with autonomy_ready: true, the other against a spec with autonomy_ready: false (or the field absent entirely). How does autonomy_ready change the outcome, and does it ever override a clear-cut case?",
    "assertions": [
      { "type": "contains", "value": "autonomy_ready" },
      { "type": "semantic", "value": "autonomy_ready: true nudges the borderline ambiguity toward deciding and recording a new Assumption Authorization entry, while false or absent nudges the same borderline case toward interviewing at the next checkpoint instead; it is only a weighting signal, not a hard gate — a clear-cut ambiguity that plainly touches a risk-keyword path or architectural-surface marker still gets interviewed even when autonomy_ready is true, and a plainly low-risk ambiguity still gets decided even when it's false or absent" }
    ]
  },
  {
    "name": "build-early-draft-pr",
    "prompt": "Run /build specs/rate-limiter.md. Stage 4 is about to dispatch its first implementation subagent. /speccy's post-approval handoff bundle already opened a draft PR for this branch. Does /build still call create-pr.sh --draft here, and does it create a second PR?",
    "assertions": [
      { "type": "contains", "value": "draft" },
      { "type": "regex", "value": "reus", "flags": "i" },
      { "type": "semantic", "value": "Before dispatching the first implementation subagent, /build still calls skills/ship/scripts/create-pr.sh --draft directly, but this is now an idempotent backstop rather than the PR's origin: since /speccy's handoff bundle already opened the draft PR, create-pr.sh finds and reuses it (reused=true, no duplicate PR — AC-005). /build only actually creates a PR in the degraded case where the bundle's PR step failed. Either way every subsequent checkpoint-interview comment and evidence artifact posts against that draft PR" }
    ]
  },
  {
    "name": "build-parallel-consistency-check",
    "prompt": "ARCH_REPORT identifies a parallel_group of 3 independent implementer subagents for Stage 4. After they all complete, what happens before the next dependent group (or Stage 5) starts? Is this the same thing as Stage 5's code review?",
    "assertions": [
      { "type": "regex", "value": "(consisten|drift)", "flags": "i" },
      { "type": "semantic", "value": "One lightweight subagent checks whether shared identifiers touched by the group's files — spec REQ/AC citations, renamed section headings, shared terminology — still agree with what sibling files in this or already-completed groups assume, since isolated-context parallel agents can't see each other's changes. This is explicitly not a substantive spec-compliance or code-quality review (that's still Stage 5) — it's a cheap, one-dispatch-per-group check for cross-file drift, skipped entirely when a group has only one implementer" }
    ]
  }
]
