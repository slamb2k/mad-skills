[
  {
    "name": "speccy-invocation-banner",
    "prompt": "Run the speccy skill with no arguments",
    "assertions": [
      {
        "type": "contains",
        "value": "██"
      },
      {
        "type": "regex",
        "value": "(Tell me everything|think this through|wreck it|Interview mode|great questions|Measure twice|assumption left behind|Precision engineering)"
      }
    ]
  },
  {
    "name": "speccy-interview-categories",
    "prompt": "What question categories does the speccy skill use?",
    "assertions": [
      {
        "type": "contains",
        "value": "Architecture"
      },
      {
        "type": "contains",
        "value": "Requirements"
      },
      {
        "type": "contains",
        "value": "Security"
      }
    ]
  },
  {
    "name": "speccy-superpowers-present",
    "prompt": "The superpowers plugin is installed. Run speccy to spec out a rate limiter. Which stage does speccy defer, and to which superpowers skill?",
    "assertions": [
      {
        "type": "contains",
        "value": "⚡ Superpowers detected"
      },
      {
        "type": "contains",
        "value": "brainstorming"
      }
    ]
  },
  {
    "name": "speccy-superpowers-absent",
    "prompt": "Superpowers is NOT installed. How does speccy gather requirements to build a spec?",
    "assertions": [
      {
        "type": "contains",
        "value": "interview"
      }
    ]
  },
  {
    "name": "speccy-location-mismatch",
    "prompt": "Run /speccy. Before Stage 1: Context Gathering, its Pre-Spec Location Check finds that Bash's shell cwd resolves to a different git toplevel than this session's declared working directory — Bash is inside a leftover worktree. What does the Pre-Spec Location Check do?",
    "assertions": [
      {
        "type": "semantic",
        "value": "Treats the toplevel mismatch as something to warn about rather than silently continuing — it does not just proceed with Stage 1 as if nothing happened"
      }
    ]
  },
  {
    "name": "speccy-eligibility-full-pass-zero-interview",
    "prompt": "Run /speccy --auto \"fix the null check in scripts/lib/eval-response.js\" — a keyword/glob exploration finds 1 plausibly-touched file, it is not on the risk-keyword-path list or the architectural-surface list, the ticket text starts with an allowed action verb, contains no hedge/uncertainty language, and resolves a concrete symbol match. What does /speccy --auto do, and what does the resulting spec look like?",
    "assertions": [
      {
        "type": "semantic",
        "value": "The ticket passes all four eligibility dimensions (scope, risk-keyword paths, architectural surface, ticket clarity), so /speccy --auto runs zero-interview inference with no interview rounds at all: a subagent generates the spec from the small-task template, populates an Autonomous Inference Assessment section stating which checks passed and why, records every substantive inferred decision as an Assumption Authorization entry, and sets autonomy_ready: true in the frontmatter"
      }
    ]
  },
  {
    "name": "speccy-auto-fallback-completeness-gate-pass",
    "prompt": "Run /speccy --auto for a ticket that fails the eligibility gate (it touches more than 3 plausibly-touched files). /speccy --auto falls back to a real interactive interview, which completes with clear answers on outcome, architecture, non-goals, definition-of-done, roadmap, and risks, and every ambiguity has a resolution. What does the fallback completeness gate self-review do, and what value is written for autonomy_ready?",
    "assertions": [
      {
        "type": "semantic",
        "value": "Eligibility failure never blocks or errors out — /speccy --auto falls back to the same Stage 1-2 interactive interview and produces a full-template spec, not the small-task template. Because the interview satisfies every completeness-gate item (outcome, architecture/approach, non-goals, definition-of-done, roadmap, risks, and a resolution per ambiguity), the gate self-review passes and the spec is written with autonomy_ready: true in its frontmatter"
      }
    ]
  },
  {
    "name": "speccy-auto-fallback-completeness-gate-fail",
    "prompt": "Run /speccy --auto for a ticket that fails the eligibility gate, so it falls back to a real interview. Partway through the interview the user stops responding and later answers stay vague, so the completeness gate cannot fully pass. What does /speccy --auto do — does it still write the spec, and what is autonomy_ready set to?",
    "assertions": [
      {
        "type": "semantic",
        "value": "The spec is still written even though the fallback completeness gate did not fully pass — a failed gate never blocks spec creation — but it is marked autonomy_ready: false rather than true, so the spec remains usable by ordinary interactive /build but is rejected by an autonomy_ready precondition"
      }
    ]
  },
  {
    "name": "speccy-eligibility-scope-fail-fallback",
    "prompt": "Run /speccy --auto for a ticket whose keyword/glob exploration finds 4 plausibly-touched files — the scope check allows at most 3 — even though the risk-keyword-path, architectural-surface, and ticket-clarity dimensions would all pass. What happens?",
    "assertions": [
      {
        "type": "semantic",
        "value": "The scope dimension alone failing is enough to fail the whole eligibility gate, since all four dimensions must pass; /speccy --auto falls back to the real interactive interview rather than running zero-interview inference. This fallback is graceful, per REQ-006 — never a hard error or a rejected ticket"
      }
    ]
  },
  {
    "name": "speccy-eligibility-risk-keyword-fail-fallback",
    "prompt": "Run /speccy --auto for a ticket whose exploration matches exactly one file, but that file falls on the risk-keyword-path list in references/autonomous-review-thresholds.md, even though scope, architectural surface, and ticket clarity would all pass. What happens?",
    "assertions": [
      {
        "type": "semantic",
        "value": "The risk-keyword-paths dimension fails because the matched file is on the risk-keyword-path list, so the eligibility gate fails overall even though the other three dimensions pass; /speccy --auto falls back to the real interactive interview instead of zero-interview inference, and this fallback is not treated as an error"
      }
    ]
  },
  {
    "name": "speccy-eligibility-architectural-surface-fail-fallback",
    "prompt": "Run /speccy --auto for a ticket whose exploration matches exactly one file, but that file is a public/exported interface per references/autonomous-architecture-surface-markers.md, even though scope, risk-keyword paths, and ticket clarity would all pass. What happens?",
    "assertions": [
      {
        "type": "semantic",
        "value": "The architectural-surface dimension fails because the matched file is a public/exported interface (a schema/migration file or shared cross-cutting state module would fail it the same way), so the eligibility gate fails overall; /speccy --auto falls back to the real interactive interview rather than zero-interview inference, again not treated as an error or a hard block"
      }
    ]
  },
  {
    "name": "speccy-eligibility-ticket-clarity-fail-fallback",
    "prompt": "Run /speccy --auto for the ticket text \"maybe explore options for improving the settings page\" — scope, risk-keyword paths, and architectural surface would all pass. What does the ticket-clarity check do with this text, and what happens overall?",
    "assertions": [
      {
        "type": "semantic",
        "value": "Ticket clarity fails: the text contains hedge/uncertainty language ('maybe', 'explore options for') and has no allowed action verb at or near the start, so the mechanical sub-checks (verb_present, no_hedge_language) fail. A single failing clarity sub-check is enough to fail the whole eligibility gate, so /speccy --auto falls back to the real interactive interview rather than zero-interview inference"
      }
    ]
  },
  {
    "name": "speccy-auto-bundled-handoff",
    "prompt": "Run /speccy --auto \"add dark mode toggle to settings page\". When does speccy create the git worktree and branch, relative to the interview/inference and the spec being approved?",
    "assertions": [
      {
        "type": "semantic",
        "value": "No worktree, branch, commit, or PR is created during the eligibility gate, inference, or interview — /speccy --auto runs entirely in the plain working directory and only at the approval moment (zero-interview inference completes and passes, or the Decision Summary is confirmed) runs the handoff bundle that creates the worktree and branch. Worktree creation is not the literal first action; it happens at approval, bundled with sync, spec commit, push, and draft PR."
      }
    ]
  },
  {
    "name": "speccy-interactive-bundled-handoff",
    "prompt": "Run /speccy (no --auto flag) to spec out a new feature. Does it create a git worktree before or during the interview, or only after the spec is approved?",
    "assertions": [
      {
        "type": "semantic",
        "value": "Interactive (non---auto) /speccy creates no git state during the interview — the worktree and branch are created only at the approval moment (when the Decision Summary is confirmed) by the same post-approval handoff bundle used in --auto mode. Both modes defer all git state to approval; neither creates the worktree as a first action."
      }
    ]
  },
  {
    "name": "speccy-bundle-no-git-state-mid-interview",
    "prompt": "During a /speccy run, the interview (or --auto inference) is still in progress and the spec has not been approved yet. What git state exists at this point — is there a worktree, branch, commit, or PR?",
    "assertions": [
      {
        "type": "semantic",
        "value": "No worktree, no branch, no commit, and no PR exist while the interview or inference is still in progress — /speccy creates zero git state before the approval moment. Everything runs in the plain invoking working directory until the spec is approved."
      }
    ]
  },
  {
    "name": "speccy-bundle-blocking-step-failure",
    "prompt": "During /speccy's post-approval handoff bundle, the push step (bundle step 6) fails — the remote rejects the push. Steps 1-5 (freeze, sync, worktree+branch, materialize spec, commit) already succeeded. Does the bundle write the pending-build marker, and how does it report the failure?",
    "assertions": [
      {
        "type": "semantic",
        "value": "Steps 1-6 are blocking, so the bundle aborts on the push failure, names the exact failed step, and leaves the already-succeeded state (freeze, sync, worktree, branch, commit) in place for inspection. It does NOT write the pending-build marker on a step-1-6 failure."
      },
      {
        "type": "regex",
        "value": "(push|step 6)"
      }
    ]
  },
  {
    "name": "speccy-bundle-degraded-pr",
    "prompt": "During /speccy's post-approval handoff bundle, the draft-PR step (bundle step 7) fails because gh is not installed. Steps 1-6 all succeeded (freeze, sync, worktree, branch, commit, push). What does the bundle do about the pending-build marker and the failed PR step?",
    "assertions": [
      {
        "type": "semantic",
        "value": "Step 7 is best-effort and degrades rather than blocking: the worktree, branch, commit, and push all stand, the pending-build marker IS still written (step 8), and the report includes the exact create-pr.sh retry command so the PR can be created later. /build's own Early-draft-PR backstop will create the PR when it runs."
      },
      {
        "type": "contains",
        "value": "create-pr.sh"
      }
    ]
  },
  {
    "name": "speccy-interactive-autonomy-ready-true",
    "prompt": "Run /speccy (no --auto flag) to spec out a feature. The interactive interview covers outcome, architecture, non-goals, definition-of-done, roadmap, and risks, and every ambiguity raised gets a resolution. Even though --auto was never passed, what does Stage 3's self-review do, and what value is written for autonomy_ready?",
    "assertions": [
      {
        "type": "semantic",
        "value": "Interactive /speccy applies the same completeness gate as --auto's fallback path in its own Stage 3 self-review; because every gate item is satisfied, the resulting spec's frontmatter is autonomy_ready: true even though --auto was never used — the gate is no longer --auto-exclusive"
      }
    ]
  }
]
